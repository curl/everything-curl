name: make

on:
  # Trigger the workflow on push or pull requests, but only for the
  # master branch
  push:
    branches:
    - master
    - '*/ci'
  pull_request:
    branches:
    - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: install prereqs
      run: sudo apt-get install proselint
      
    - name: store proselint config
      run: |
        echo '{' > .proselint
        echo '   "checks": {' >> .proselint
        echo '     "typography.diacritical_marks": false,' >> .proselint
        echo '     "typography.symbols": false,' >> .proselint
        echo '     "annotations.misc": false' >> .proselint
        echo '   }' >> .proselint
        echo '}' >> .proselint

    - name: check prose
      run: make check
