# Multi handle

All easy handles added to the same multi handle automatically share
[connection cache](../transfers/conn/reuse.md) and [dns
cache](../transfers/conn/names.md).

You can however still make individual easy handles share data when used in a
multi transfer if the default setup is not sufficient.
